# Yoga Assistant : A system for Yoga Pose Detection and Correction <br>
User can select the available yoga poses from the UI and can perform that selected pose. The system detects the yoga pose the user is performing and provides correction guidance through voice feedback for correction the pose. This system uses is trained on 3 different models RFC, CNN and more.
